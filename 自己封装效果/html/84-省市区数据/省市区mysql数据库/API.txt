1.parent_id = 100000 的数据是省级数据
2.数据的 parent_id 是其父级区域的 id,也即数据的 id 是其子级区域的 parent_id
3.level = 1 的数据 是 省级 数据；
  level = 2 的数据 是 市级 数据；
  level = 3 的数据 是 县/区级 数据；
4.city_code 是地区所对应的 电话号码 区号
5.zip_code 是地区所对应的邮政编码 
6. merger_name是地区所对应的 汉语省市区的全称
7.pinyin 是地区的汉语拼音
8.state 可以设置 所对应的项目 的使用区域，因为一个项目 可能并不是对全国各个地区实用的，这样就可以通过设定这个状态来 设置使用的区域
9.
